type: object
properties:
  date:
    type: string
    description: Date holds the value of the "date" field.
  edges:
    type: object
    description: >-
      Edges holds the relations/edges for other nodes in the graph.

      The values are being populated by the ReservationQuery when eager-loading
      is set.
    allOf:
      - $ref: '#/definitions/placio-app_ent.ReservationEdges'
  id:
    type: string
    description: ID of the ent.
  numberOfPeople:
    type: integer
    description: NumberOfPeople holds the value of the "numberOfPeople" field.
  status:
    type: string
    description: Status holds the value of the "status" field.
  time:
    type: string
    description: Time holds the value of the "time" field.
