// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"placio-app/ent/fitness"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Fitness is the model entity for the Fitness schema.
type Fitness struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name         string `json:"name,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Fitness) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case fitness.FieldID, fitness.FieldName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Fitness fields.
func (f *Fitness) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case fitness.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				f.ID = value.String
			}
		case fitness.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				f.Name = value.String
			}
		default:
			f.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Fitness.
// This includes values selected through modifiers, order, etc.
func (f *Fitness) Value(name string) (ent.Value, error) {
	return f.selectValues.Get(name)
}

// Update returns a builder for updating this Fitness.
// Note that you need to call Fitness.Unwrap() before calling this method if this Fitness
// was returned from a transaction, and the transaction was committed or rolled back.
func (f *Fitness) Update() *FitnessUpdateOne {
	return NewFitnessClient(f.config).UpdateOne(f)
}

// Unwrap unwraps the Fitness entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (f *Fitness) Unwrap() *Fitness {
	_tx, ok := f.config.driver.(*txDriver)
	if !ok {
		panic("ent: Fitness is not a transactional entity")
	}
	f.config.driver = _tx.drv
	return f
}

// String implements the fmt.Stringer.
func (f *Fitness) String() string {
	var builder strings.Builder
	builder.WriteString("Fitness(")
	builder.WriteString(fmt.Sprintf("id=%v, ", f.ID))
	builder.WriteString("name=")
	builder.WriteString(f.Name)
	builder.WriteByte(')')
	return builder.String()
}

// Fitnesses is a parsable slice of Fitness.
type Fitnesses []*Fitness
