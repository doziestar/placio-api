// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"placio-app/ent/order"
	"placio-app/ent/place"
	"placio-app/ent/table"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TableCreate is the builder for creating a Table entity.
type TableCreate struct {
	config
	mutation *TableMutation
	hooks    []Hook
}

// SetNumber sets the "number" field.
func (tc *TableCreate) SetNumber(i int) *TableCreate {
	tc.mutation.SetNumber(i)
	return tc
}

// SetQrCode sets the "qr_code" field.
func (tc *TableCreate) SetQrCode(s string) *TableCreate {
	tc.mutation.SetQrCode(s)
	return tc
}

// SetNillableQrCode sets the "qr_code" field if the given value is not nil.
func (tc *TableCreate) SetNillableQrCode(s *string) *TableCreate {
	if s != nil {
		tc.SetQrCode(*s)
	}
	return tc
}

// SetID sets the "id" field.
func (tc *TableCreate) SetID(s string) *TableCreate {
	tc.mutation.SetID(s)
	return tc
}

// SetPlaceID sets the "place" edge to the Place entity by ID.
func (tc *TableCreate) SetPlaceID(id string) *TableCreate {
	tc.mutation.SetPlaceID(id)
	return tc
}

// SetNillablePlaceID sets the "place" edge to the Place entity by ID if the given value is not nil.
func (tc *TableCreate) SetNillablePlaceID(id *string) *TableCreate {
	if id != nil {
		tc = tc.SetPlaceID(*id)
	}
	return tc
}

// SetPlace sets the "place" edge to the Place entity.
func (tc *TableCreate) SetPlace(p *Place) *TableCreate {
	return tc.SetPlaceID(p.ID)
}

// AddOrderIDs adds the "orders" edge to the Order entity by IDs.
func (tc *TableCreate) AddOrderIDs(ids ...string) *TableCreate {
	tc.mutation.AddOrderIDs(ids...)
	return tc
}

// AddOrders adds the "orders" edges to the Order entity.
func (tc *TableCreate) AddOrders(o ...*Order) *TableCreate {
	ids := make([]string, len(o))
	for i := range o {
		ids[i] = o[i].ID
	}
	return tc.AddOrderIDs(ids...)
}

// Mutation returns the TableMutation object of the builder.
func (tc *TableCreate) Mutation() *TableMutation {
	return tc.mutation
}

// Save creates the Table in the database.
func (tc *TableCreate) Save(ctx context.Context) (*Table, error) {
	return withHooks(ctx, tc.sqlSave, tc.mutation, tc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (tc *TableCreate) SaveX(ctx context.Context) *Table {
	v, err := tc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tc *TableCreate) Exec(ctx context.Context) error {
	_, err := tc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tc *TableCreate) ExecX(ctx context.Context) {
	if err := tc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (tc *TableCreate) check() error {
	if _, ok := tc.mutation.Number(); !ok {
		return &ValidationError{Name: "number", err: errors.New(`ent: missing required field "Table.number"`)}
	}
	if v, ok := tc.mutation.ID(); ok {
		if err := table.IDValidator(v); err != nil {
			return &ValidationError{Name: "id", err: fmt.Errorf(`ent: validator failed for field "Table.id": %w`, err)}
		}
	}
	return nil
}

func (tc *TableCreate) sqlSave(ctx context.Context) (*Table, error) {
	if err := tc.check(); err != nil {
		return nil, err
	}
	_node, _spec := tc.createSpec()
	if err := sqlgraph.CreateNode(ctx, tc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(string); ok {
			_node.ID = id
		} else {
			return nil, fmt.Errorf("unexpected Table.ID type: %T", _spec.ID.Value)
		}
	}
	tc.mutation.id = &_node.ID
	tc.mutation.done = true
	return _node, nil
}

func (tc *TableCreate) createSpec() (*Table, *sqlgraph.CreateSpec) {
	var (
		_node = &Table{config: tc.config}
		_spec = sqlgraph.NewCreateSpec(table.Table, sqlgraph.NewFieldSpec(table.FieldID, field.TypeString))
	)
	if id, ok := tc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := tc.mutation.Number(); ok {
		_spec.SetField(table.FieldNumber, field.TypeInt, value)
		_node.Number = value
	}
	if value, ok := tc.mutation.QrCode(); ok {
		_spec.SetField(table.FieldQrCode, field.TypeString, value)
		_node.QrCode = value
	}
	if nodes := tc.mutation.PlaceIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   table.PlaceTable,
			Columns: []string{table.PlaceColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(place.FieldID, field.TypeString),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.place_tables = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := tc.mutation.OrdersIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2M,
			Inverse: false,
			Table:   table.OrdersTable,
			Columns: table.OrdersPrimaryKey,
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(order.FieldID, field.TypeString),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// TableCreateBulk is the builder for creating many Table entities in bulk.
type TableCreateBulk struct {
	config
	err      error
	builders []*TableCreate
}

// Save creates the Table entities in the database.
func (tcb *TableCreateBulk) Save(ctx context.Context) ([]*Table, error) {
	if tcb.err != nil {
		return nil, tcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(tcb.builders))
	nodes := make([]*Table, len(tcb.builders))
	mutators := make([]Mutator, len(tcb.builders))
	for i := range tcb.builders {
		func(i int, root context.Context) {
			builder := tcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TableMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, tcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, tcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, tcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (tcb *TableCreateBulk) SaveX(ctx context.Context) []*Table {
	v, err := tcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tcb *TableCreateBulk) Exec(ctx context.Context) error {
	_, err := tcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tcb *TableCreateBulk) ExecX(ctx context.Context) {
	if err := tcb.Exec(ctx); err != nil {
		panic(err)
	}
}
