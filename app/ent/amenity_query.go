



// Code generated by ent, DO NOT EDIT.



package ent



	
import (
	"context"
	"errors"
	"fmt"
	"math"
	"strings"
	"sync"
	"time"
		"placio-app/ent/predicate"
	"entgo.io/ent"
	"entgo.io/ent/dialect"
			"database/sql/driver"
			"entgo.io/ent/dialect/sql"
			"entgo.io/ent/dialect/sql/sqlgraph"
			"entgo.io/ent/dialect/sql/sqljson"
			"entgo.io/ent/schema/field"
			 "placio-app/ent/amenity"
			 "placio-app/ent/place"

)






// AmenityQuery is the builder for querying Amenity entities.
type AmenityQuery struct {
	config
	ctx			*QueryContext
	order		[]amenity.OrderOption
	inters		[]Interceptor
	predicates 	[]predicate.Amenity
		withPlaces *PlaceQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AmenityQuery builder.
func (aq *AmenityQuery) Where(ps ...predicate.Amenity) *AmenityQuery {
	aq.predicates = append(aq.predicates, ps...)
	return aq
}

// Limit the number of records to be returned by this query.
func (aq *AmenityQuery) Limit(limit int) *AmenityQuery {
	aq.ctx.Limit = &limit
	return aq
}

// Offset to start from.
func (aq *AmenityQuery) Offset(offset int) *AmenityQuery {
	aq.ctx.Offset = &offset
	return aq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aq *AmenityQuery) Unique(unique bool) *AmenityQuery {
	aq.ctx.Unique = &unique
	return aq
}

// Order specifies how the records should be ordered.
func (aq *AmenityQuery) Order(o ...amenity.OrderOption) *AmenityQuery {
	aq.order = append(aq.order, o...)
	return aq
}



	
	// QueryPlaces chains the current query on the "places" edge.
	func (aq *AmenityQuery) QueryPlaces() *PlaceQuery {
		query := (&PlaceClient{config: aq.config}).Query()
		query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
			if err := aq.prepareQuery(ctx); err != nil {
				return nil, err
			}  
	selector := aq.sqlQuery(ctx)
	if err := selector.Err(); err != nil {
		return nil, err
	}
	step := sqlgraph.NewStep(
		sqlgraph.From(amenity.Table, amenity.FieldID, selector),
		sqlgraph.To(place.Table, place.FieldID),
		sqlgraph.Edge(sqlgraph.M2M, false, amenity.PlacesTable,amenity.PlacesPrimaryKey...),
	)
	fromU = sqlgraph.SetNeighbors(aq.driver.Dialect(), step)
return fromU, nil
		}
		return query
	}


// First returns the first Amenity entity from the query. 
// Returns a *NotFoundError when no Amenity was found.
func (aq *AmenityQuery) First(ctx context.Context) (*Amenity, error) {
	nodes, err := aq.Limit(1).All(setContextOp(ctx, aq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ amenity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aq *AmenityQuery) FirstX(ctx context.Context) *Amenity {
	node, err := aq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}


	// FirstID returns the first Amenity ID from the query.
	// Returns a *NotFoundError when no Amenity ID was found.
	func (aq *AmenityQuery) FirstID(ctx context.Context) (id string, err error) {
		var ids []string
		if ids, err = aq.Limit(1).IDs(setContextOp(ctx, aq.ctx, "FirstID")); err != nil {
			return
		}
		if len(ids) == 0 {
			err = &NotFoundError{ amenity.Label}
			return
		}
		return ids[0], nil
	}

	// FirstIDX is like FirstID, but panics if an error occurs.
	func (aq *AmenityQuery) FirstIDX(ctx context.Context) string {
		id, err := aq.FirstID(ctx)
		if err != nil && !IsNotFound(err) {
			panic(err)
		}
		return id
	}


// Only returns a single Amenity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Amenity entity is found.
// Returns a *NotFoundError when no Amenity entities are found.
func (aq *AmenityQuery) Only(ctx context.Context) (*Amenity, error) {
	nodes, err := aq.Limit(2).All(setContextOp(ctx, aq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ amenity.Label}
	default:
		return nil, &NotSingularError{ amenity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aq *AmenityQuery) OnlyX(ctx context.Context) *Amenity {
	node, err := aq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}


	// OnlyID is like Only, but returns the only Amenity ID in the query.
	// Returns a *NotSingularError when more than one Amenity ID is found.
	// Returns a *NotFoundError when no entities are found.
	func (aq *AmenityQuery) OnlyID(ctx context.Context) (id string, err error) {
		var ids []string
		if ids, err = aq.Limit(2).IDs(setContextOp(ctx, aq.ctx, "OnlyID")); err != nil {
			return
		}
		switch len(ids) {
		case 1:
			id = ids[0]
		case 0:
			err = &NotFoundError{ amenity.Label}
		default:
			err = &NotSingularError{ amenity.Label}
		}
		return
	}

	// OnlyIDX is like OnlyID, but panics if an error occurs.
	func (aq *AmenityQuery) OnlyIDX(ctx context.Context) string {
		id, err := aq.OnlyID(ctx)
		if err != nil {
			panic(err)
		}
		return id
	}


// All executes the query and returns a list of Amenities.
func (aq *AmenityQuery) All(ctx context.Context) ([]*Amenity, error) {
	ctx = setContextOp(ctx, aq.ctx, "All")
	if err := aq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Amenity, *AmenityQuery]()
	return withInterceptors[[]*Amenity](ctx, aq, qr, aq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aq *AmenityQuery) AllX(ctx context.Context) []*Amenity {
	nodes, err := aq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}


	// IDs executes the query and returns a list of Amenity IDs.
	func (aq *AmenityQuery) IDs(ctx context.Context) (ids []string, err error) {
		if aq.ctx.Unique == nil && aq.path != nil {
			aq.Unique(true)
		}
		ctx = setContextOp(ctx, aq.ctx, "IDs")
		if err = aq.Select(amenity.FieldID).Scan(ctx, &ids); err != nil {
			return nil, err
		}
		return ids, nil
	}

	// IDsX is like IDs, but panics if an error occurs.
	func (aq *AmenityQuery) IDsX(ctx context.Context) []string {
		ids, err := aq.IDs(ctx)
		if err != nil {
			panic(err)
		}
		return ids
	}


// Count returns the count of the given query.
func (aq *AmenityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aq.ctx, "Count")
	if err := aq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aq, querierCount[*AmenityQuery](), aq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aq *AmenityQuery) CountX(ctx context.Context) int {
	count, err := aq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aq *AmenityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aq.ctx, "Exist")
	switch _, err := aq.FirstID(ctx);{
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aq *AmenityQuery) ExistX(ctx context.Context) bool {
	exist, err := aq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AmenityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aq *AmenityQuery) Clone() *AmenityQuery {
	if aq == nil {
		return nil
	}
	return &AmenityQuery{
		config: 	aq.config,
		ctx: 		aq.ctx.Clone(),
		order: 		append([]amenity.OrderOption{}, aq.order...),
		inters: 	append([]Interceptor{}, aq.inters...),
		predicates: append([]predicate.Amenity{}, aq.predicates...),
			withPlaces: aq.withPlaces.Clone(),
		// clone intermediate query.
		sql: aq.sql.Clone(),
		path: aq.path,
	}
}
	
	
	// WithPlaces tells the query-builder to eager-load the nodes that are connected to
	// the "places" edge. The optional arguments are used to configure the query builder of the edge.
	func (aq *AmenityQuery) WithPlaces(opts ...func(*PlaceQuery)) *AmenityQuery {
		query := (&PlaceClient{config: aq.config}).Query()
		for _, opt := range opts {
			opt(query)
		}
		aq.withPlaces = query
		return aq
	}



// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Amenity.Query().
//		GroupBy(amenity.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (aq *AmenityQuery) GroupBy(field string, fields ...string) *AmenityGroupBy {
	aq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AmenityGroupBy{build: aq}
	grbuild.flds = &aq.ctx.Fields
	grbuild.label = amenity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}



// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Amenity.Query().
//		Select(amenity.FieldName).
//		Scan(ctx, &v)
//
func (aq *AmenityQuery) Select(fields ...string) *AmenitySelect {
	aq.ctx.Fields = append(aq.ctx.Fields, fields...)
	sbuild := &AmenitySelect{ AmenityQuery: aq }
	sbuild.label = amenity.Label
	sbuild.flds, sbuild.scan = &aq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AmenitySelect configured with the given aggregations.
func (aq *AmenityQuery) Aggregate(fns ...AggregateFunc) *AmenitySelect {
	return aq.Select().Aggregate(fns...)
}

func (aq *AmenityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aq); err != nil {
				return err
			}
		}
	}
	for _, f := range aq.ctx.Fields {
		if !amenity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aq.path != nil {
		prev, err := aq.path(ctx)
		if err != nil {
			return err
		}
		aq.sql = prev
	}
	return nil
}


	
	




func (aq *AmenityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Amenity, error) {
	var (
		nodes = []*Amenity{}
		_spec = aq.querySpec()
			loadedTypes = [1]bool{
					aq.withPlaces != nil,
			}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Amenity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Amenity{config: aq.config}
		nodes = append(nodes, node)
			node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
		if query := aq.withPlaces; query != nil {
			if err := aq.loadPlaces(ctx, query, nodes, 
				func(n *Amenity){ n.Edges.Places = []*Place{} },
				func(n *Amenity, e *Place){ n.Edges.Places = append(n.Edges.Places, e) }); err != nil {
				return nil, err
			}
		}
	return nodes, nil
}


	func (aq *AmenityQuery) loadPlaces(ctx context.Context, query *PlaceQuery, nodes []*Amenity, init func(*Amenity), assign func(*Amenity, *Place)) error {
			edgeIDs := make([]driver.Value, len(nodes))
			byID := make(map[string]*Amenity)
			nids := make(map[string]map[*Amenity]struct{})
			for i, node := range nodes {
				edgeIDs[i] = node.ID
				byID[node.ID] = node
				if init != nil {
					init(node)
				}
			}
			query.Where(func(s *sql.Selector) {
				joinT := sql.Table(amenity.PlacesTable)
				s.Join(joinT).On(s.C(place.FieldID), joinT.C(amenity.PlacesPrimaryKey[1]))
				s.Where(sql.InValues(joinT.C(amenity.PlacesPrimaryKey[0]), edgeIDs...))
				columns := s.SelectedColumns()
				s.Select(joinT.C(amenity.PlacesPrimaryKey[0]))
				s.AppendSelect(columns...)
				s.SetDistinct(false)
			})
			if err := query.prepareQuery(ctx); err != nil {
				return err
			}
			qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
				return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
					assign := spec.Assign
					values := spec.ScanValues
					spec.ScanValues = func(columns []string) ([]any, error) {
						values, err := values(columns[1:])
						if err != nil {
							return nil, err
						}
						return append([]any{new(sql.NullString)}, values...), nil
					}
					spec.Assign = func(columns []string, values []any) error {
						outValue := values[0].(*sql.NullString).String
						inValue := values[1].(*sql.NullString).String
						if nids[inValue] == nil {
							nids[inValue] = map[*Amenity]struct{}{byID[outValue]: {}}
							return assign(columns[1:], values[1:])
						}
						nids[inValue][byID[outValue]] = struct{}{}
						return nil
					}
				})
			})
			neighbors, err :=  withInterceptors[[]*Place](ctx, query, qr, query.inters)
			if err != nil {
				return err
			}
			for _, n := range neighbors {
				nodes, ok := nids[n.ID]
				if !ok {
					return fmt.Errorf(`unexpected "places" node returned %v`, n.ID)
				}
				for kn := range nodes {
					assign(kn, n)
				}
			}
		return nil
	}

func (aq *AmenityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aq.querySpec()
		_spec.Node.Columns = aq.ctx.Fields
		if len(aq.ctx.Fields) > 0 {
			_spec.Unique = aq.ctx.Unique != nil && *aq.ctx.Unique
		}
	return sqlgraph.CountNodes(ctx, aq.driver, _spec)
}

func (aq *AmenityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(amenity.Table, amenity.Columns, sqlgraph.NewFieldSpec(amenity.FieldID, field.TypeString))
	_spec.From = aq.sql
	if unique := aq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aq.path != nil {
		_spec.Unique = true
	}
	if fields := aq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
			_spec.Node.Columns = append(_spec.Node.Columns, amenity.FieldID)
			for i := range fields {
				if fields[i] != amenity.FieldID {
					_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
				}
			}
	}
	if ps := aq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}





func (aq *AmenityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aq.driver.Dialect())
	t1 := builder.Table(amenity.Table)
	columns := aq.ctx.Fields
	if len(columns) == 0 {
		columns = amenity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aq.sql != nil {
		selector = aq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aq.ctx.Unique != nil && *aq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range aq.predicates {
		p(selector)
	}
	for _, p := range aq.order {
		p(selector)
	}
	if offset := aq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

    

    











// AmenityGroupBy is the group-by builder for Amenity entities.
type AmenityGroupBy struct {
	selector
	build *AmenityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (agb *AmenityGroupBy) Aggregate(fns ...AggregateFunc) *AmenityGroupBy {
	agb.fns = append(agb.fns, fns...)
	return agb
}

// Scan applies the selector query and scans the result into the given value.
func (agb *AmenityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, agb.build.ctx, "GroupBy")
	if err := agb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AmenityQuery, *AmenityGroupBy](ctx, agb.build, agb, agb.build.inters, v)
}


	
	



func (agb *AmenityGroupBy) sqlScan(ctx context.Context, root *AmenityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(agb.fns))
	for _, fn := range agb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*agb.flds) + len(agb.fns))
		for _, f := range *agb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*agb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := agb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}







// AmenitySelect is the builder for selecting fields of Amenity entities.
type AmenitySelect struct {
	*AmenityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (as *AmenitySelect) Aggregate(fns ...AggregateFunc) *AmenitySelect {
	as.fns = append(as.fns, fns...)
	return as
}

// Scan applies the selector query and scans the result into the given value.
func (as *AmenitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, as.ctx, "Select")
	if err := as.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AmenityQuery, *AmenitySelect](ctx, as.AmenityQuery, as, as.inters, v)
}


	
	



func (as *AmenitySelect) sqlScan(ctx context.Context, root *AmenityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(as.fns))
	for _, fn := range as.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*as.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := as.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}



    






