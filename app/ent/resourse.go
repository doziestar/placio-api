// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"placio-app/ent/resourse"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Resourse is the model entity for the Resourse schema.
type Resourse struct {
	config
	// ID of the ent.
	ID           string `json:"id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Resourse) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case resourse.FieldID:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Resourse fields.
func (r *Resourse) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case resourse.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				r.ID = value.String
			}
		default:
			r.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Resourse.
// This includes values selected through modifiers, order, etc.
func (r *Resourse) Value(name string) (ent.Value, error) {
	return r.selectValues.Get(name)
}

// Update returns a builder for updating this Resourse.
// Note that you need to call Resourse.Unwrap() before calling this method if this Resourse
// was returned from a transaction, and the transaction was committed or rolled back.
func (r *Resourse) Update() *ResourseUpdateOne {
	return NewResourseClient(r.config).UpdateOne(r)
}

// Unwrap unwraps the Resourse entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (r *Resourse) Unwrap() *Resourse {
	_tx, ok := r.config.driver.(*txDriver)
	if !ok {
		panic("ent: Resourse is not a transactional entity")
	}
	r.config.driver = _tx.drv
	return r
}

// String implements the fmt.Stringer.
func (r *Resourse) String() string {
	var builder strings.Builder
	builder.WriteString("Resourse(")
	builder.WriteString(fmt.Sprintf("id=%v", r.ID))
	builder.WriteByte(')')
	return builder.String()
}

// Resourses is a parsable slice of Resourse.
type Resourses []*Resourse
