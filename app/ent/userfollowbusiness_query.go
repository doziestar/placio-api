// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"placio-app/ent/business"
	"placio-app/ent/predicate"
	"placio-app/ent/user"
	"placio-app/ent/userfollowbusiness"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserFollowBusinessQuery is the builder for querying UserFollowBusiness entities.
type UserFollowBusinessQuery struct {
	config
	ctx          *QueryContext
	order        []userfollowbusiness.OrderOption
	inters       []Interceptor
	predicates   []predicate.UserFollowBusiness
	withUser     *UserQuery
	withBusiness *BusinessQuery
	withFKs      bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserFollowBusinessQuery builder.
func (ufbq *UserFollowBusinessQuery) Where(ps ...predicate.UserFollowBusiness) *UserFollowBusinessQuery {
	ufbq.predicates = append(ufbq.predicates, ps...)
	return ufbq
}

// Limit the number of records to be returned by this query.
func (ufbq *UserFollowBusinessQuery) Limit(limit int) *UserFollowBusinessQuery {
	ufbq.ctx.Limit = &limit
	return ufbq
}

// Offset to cmd from.
func (ufbq *UserFollowBusinessQuery) Offset(offset int) *UserFollowBusinessQuery {
	ufbq.ctx.Offset = &offset
	return ufbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ufbq *UserFollowBusinessQuery) Unique(unique bool) *UserFollowBusinessQuery {
	ufbq.ctx.Unique = &unique
	return ufbq
}

// Order specifies how the records should be ordered.
func (ufbq *UserFollowBusinessQuery) Order(o ...userfollowbusiness.OrderOption) *UserFollowBusinessQuery {
	ufbq.order = append(ufbq.order, o...)
	return ufbq
}

// QueryUser chains the current query on the "user" edge.
func (ufbq *UserFollowBusinessQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: ufbq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ufbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ufbq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userfollowbusiness.Table, userfollowbusiness.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userfollowbusiness.UserTable, userfollowbusiness.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(ufbq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBusiness chains the current query on the "business" edge.
func (ufbq *UserFollowBusinessQuery) QueryBusiness() *BusinessQuery {
	query := (&BusinessClient{config: ufbq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ufbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ufbq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userfollowbusiness.Table, userfollowbusiness.FieldID, selector),
			sqlgraph.To(business.Table, business.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userfollowbusiness.BusinessTable, userfollowbusiness.BusinessColumn),
		)
		fromU = sqlgraph.SetNeighbors(ufbq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserFollowBusiness entity from the query.
// Returns a *NotFoundError when no UserFollowBusiness was found.
func (ufbq *UserFollowBusinessQuery) First(ctx context.Context) (*UserFollowBusiness, error) {
	nodes, err := ufbq.Limit(1).All(setContextOp(ctx, ufbq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userfollowbusiness.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ufbq *UserFollowBusinessQuery) FirstX(ctx context.Context) *UserFollowBusiness {
	node, err := ufbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserFollowBusiness ID from the query.
// Returns a *NotFoundError when no UserFollowBusiness ID was found.
func (ufbq *UserFollowBusinessQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ufbq.Limit(1).IDs(setContextOp(ctx, ufbq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userfollowbusiness.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ufbq *UserFollowBusinessQuery) FirstIDX(ctx context.Context) string {
	id, err := ufbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserFollowBusiness entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserFollowBusiness entity is found.
// Returns a *NotFoundError when no UserFollowBusiness entities are found.
func (ufbq *UserFollowBusinessQuery) Only(ctx context.Context) (*UserFollowBusiness, error) {
	nodes, err := ufbq.Limit(2).All(setContextOp(ctx, ufbq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userfollowbusiness.Label}
	default:
		return nil, &NotSingularError{userfollowbusiness.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ufbq *UserFollowBusinessQuery) OnlyX(ctx context.Context) *UserFollowBusiness {
	node, err := ufbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserFollowBusiness ID in the query.
// Returns a *NotSingularError when more than one UserFollowBusiness ID is found.
// Returns a *NotFoundError when no entities are found.
func (ufbq *UserFollowBusinessQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ufbq.Limit(2).IDs(setContextOp(ctx, ufbq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userfollowbusiness.Label}
	default:
		err = &NotSingularError{userfollowbusiness.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ufbq *UserFollowBusinessQuery) OnlyIDX(ctx context.Context) string {
	id, err := ufbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserFollowBusinesses.
func (ufbq *UserFollowBusinessQuery) All(ctx context.Context) ([]*UserFollowBusiness, error) {
	ctx = setContextOp(ctx, ufbq.ctx, "All")
	if err := ufbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserFollowBusiness, *UserFollowBusinessQuery]()
	return withInterceptors[[]*UserFollowBusiness](ctx, ufbq, qr, ufbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ufbq *UserFollowBusinessQuery) AllX(ctx context.Context) []*UserFollowBusiness {
	nodes, err := ufbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserFollowBusiness IDs.
func (ufbq *UserFollowBusinessQuery) IDs(ctx context.Context) (ids []string, err error) {
	if ufbq.ctx.Unique == nil && ufbq.path != nil {
		ufbq.Unique(true)
	}
	ctx = setContextOp(ctx, ufbq.ctx, "IDs")
	if err = ufbq.Select(userfollowbusiness.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ufbq *UserFollowBusinessQuery) IDsX(ctx context.Context) []string {
	ids, err := ufbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ufbq *UserFollowBusinessQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ufbq.ctx, "Count")
	if err := ufbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ufbq, querierCount[*UserFollowBusinessQuery](), ufbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ufbq *UserFollowBusinessQuery) CountX(ctx context.Context) int {
	count, err := ufbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ufbq *UserFollowBusinessQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ufbq.ctx, "Exist")
	switch _, err := ufbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ufbq *UserFollowBusinessQuery) ExistX(ctx context.Context) bool {
	exist, err := ufbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserFollowBusinessQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ufbq *UserFollowBusinessQuery) Clone() *UserFollowBusinessQuery {
	if ufbq == nil {
		return nil
	}
	return &UserFollowBusinessQuery{
		config:       ufbq.config,
		ctx:          ufbq.ctx.Clone(),
		order:        append([]userfollowbusiness.OrderOption{}, ufbq.order...),
		inters:       append([]Interceptor{}, ufbq.inters...),
		predicates:   append([]predicate.UserFollowBusiness{}, ufbq.predicates...),
		withUser:     ufbq.withUser.Clone(),
		withBusiness: ufbq.withBusiness.Clone(),
		// clone intermediate query.
		sql:  ufbq.sql.Clone(),
		path: ufbq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (ufbq *UserFollowBusinessQuery) WithUser(opts ...func(*UserQuery)) *UserFollowBusinessQuery {
	query := (&UserClient{config: ufbq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ufbq.withUser = query
	return ufbq
}

// WithBusiness tells the query-builder to eager-load the nodes that are connected to
// the "business" edge. The optional arguments are used to configure the query builder of the edge.
func (ufbq *UserFollowBusinessQuery) WithBusiness(opts ...func(*BusinessQuery)) *UserFollowBusinessQuery {
	query := (&BusinessClient{config: ufbq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ufbq.withBusiness = query
	return ufbq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"CreatedAt,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserFollowBusiness.Query().
//		GroupBy(userfollowbusiness.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ufbq *UserFollowBusinessQuery) GroupBy(field string, fields ...string) *UserFollowBusinessGroupBy {
	ufbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserFollowBusinessGroupBy{build: ufbq}
	grbuild.flds = &ufbq.ctx.Fields
	grbuild.label = userfollowbusiness.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"CreatedAt,omitempty"`
//	}
//
//	client.UserFollowBusiness.Query().
//		Select(userfollowbusiness.FieldCreatedAt).
//		Scan(ctx, &v)
func (ufbq *UserFollowBusinessQuery) Select(fields ...string) *UserFollowBusinessSelect {
	ufbq.ctx.Fields = append(ufbq.ctx.Fields, fields...)
	sbuild := &UserFollowBusinessSelect{UserFollowBusinessQuery: ufbq}
	sbuild.label = userfollowbusiness.Label
	sbuild.flds, sbuild.scan = &ufbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserFollowBusinessSelect configured with the given aggregations.
func (ufbq *UserFollowBusinessQuery) Aggregate(fns ...AggregateFunc) *UserFollowBusinessSelect {
	return ufbq.Select().Aggregate(fns...)
}

func (ufbq *UserFollowBusinessQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ufbq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ufbq); err != nil {
				return err
			}
		}
	}
	for _, f := range ufbq.ctx.Fields {
		if !userfollowbusiness.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ufbq.path != nil {
		prev, err := ufbq.path(ctx)
		if err != nil {
			return err
		}
		ufbq.sql = prev
	}
	return nil
}

func (ufbq *UserFollowBusinessQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserFollowBusiness, error) {
	var (
		nodes       = []*UserFollowBusiness{}
		withFKs     = ufbq.withFKs
		_spec       = ufbq.querySpec()
		loadedTypes = [2]bool{
			ufbq.withUser != nil,
			ufbq.withBusiness != nil,
		}
	)
	if ufbq.withUser != nil || ufbq.withBusiness != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, userfollowbusiness.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserFollowBusiness).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserFollowBusiness{config: ufbq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ufbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ufbq.withUser; query != nil {
		if err := ufbq.loadUser(ctx, query, nodes, nil,
			func(n *UserFollowBusiness, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := ufbq.withBusiness; query != nil {
		if err := ufbq.loadBusiness(ctx, query, nodes, nil,
			func(n *UserFollowBusiness, e *Business) { n.Edges.Business = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ufbq *UserFollowBusinessQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*UserFollowBusiness, init func(*UserFollowBusiness), assign func(*UserFollowBusiness, *User)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*UserFollowBusiness)
	for i := range nodes {
		if nodes[i].user_followed_businesses == nil {
			continue
		}
		fk := *nodes[i].user_followed_businesses
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_followed_businesses" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ufbq *UserFollowBusinessQuery) loadBusiness(ctx context.Context, query *BusinessQuery, nodes []*UserFollowBusiness, init func(*UserFollowBusiness), assign func(*UserFollowBusiness, *Business)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*UserFollowBusiness)
	for i := range nodes {
		if nodes[i].business_follower_users == nil {
			continue
		}
		fk := *nodes[i].business_follower_users
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(business.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "business_follower_users" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ufbq *UserFollowBusinessQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ufbq.querySpec()
	_spec.Node.Columns = ufbq.ctx.Fields
	if len(ufbq.ctx.Fields) > 0 {
		_spec.Unique = ufbq.ctx.Unique != nil && *ufbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ufbq.driver, _spec)
}

func (ufbq *UserFollowBusinessQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userfollowbusiness.Table, userfollowbusiness.Columns, sqlgraph.NewFieldSpec(userfollowbusiness.FieldID, field.TypeString))
	_spec.From = ufbq.sql
	if unique := ufbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ufbq.path != nil {
		_spec.Unique = true
	}
	if fields := ufbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userfollowbusiness.FieldID)
		for i := range fields {
			if fields[i] != userfollowbusiness.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ufbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ufbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ufbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ufbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ufbq *UserFollowBusinessQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ufbq.driver.Dialect())
	t1 := builder.Table(userfollowbusiness.Table)
	columns := ufbq.ctx.Fields
	if len(columns) == 0 {
		columns = userfollowbusiness.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ufbq.sql != nil {
		selector = ufbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ufbq.ctx.Unique != nil && *ufbq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ufbq.predicates {
		p(selector)
	}
	for _, p := range ufbq.order {
		p(selector)
	}
	if offset := ufbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We cmd
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ufbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserFollowBusinessGroupBy is the group-by builder for UserFollowBusiness entities.
type UserFollowBusinessGroupBy struct {
	selector
	build *UserFollowBusinessQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ufbgb *UserFollowBusinessGroupBy) Aggregate(fns ...AggregateFunc) *UserFollowBusinessGroupBy {
	ufbgb.fns = append(ufbgb.fns, fns...)
	return ufbgb
}

// Scan applies the selector query and scans the result into the given value.
func (ufbgb *UserFollowBusinessGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ufbgb.build.ctx, "GroupBy")
	if err := ufbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserFollowBusinessQuery, *UserFollowBusinessGroupBy](ctx, ufbgb.build, ufbgb, ufbgb.build.inters, v)
}

func (ufbgb *UserFollowBusinessGroupBy) sqlScan(ctx context.Context, root *UserFollowBusinessQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ufbgb.fns))
	for _, fn := range ufbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ufbgb.flds)+len(ufbgb.fns))
		for _, f := range *ufbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ufbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ufbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserFollowBusinessSelect is the builder for selecting fields of UserFollowBusiness entities.
type UserFollowBusinessSelect struct {
	*UserFollowBusinessQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ufbs *UserFollowBusinessSelect) Aggregate(fns ...AggregateFunc) *UserFollowBusinessSelect {
	ufbs.fns = append(ufbs.fns, fns...)
	return ufbs
}

// Scan applies the selector query and scans the result into the given value.
func (ufbs *UserFollowBusinessSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ufbs.ctx, "Select")
	if err := ufbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserFollowBusinessQuery, *UserFollowBusinessSelect](ctx, ufbs.UserFollowBusinessQuery, ufbs, ufbs.inters, v)
}

func (ufbs *UserFollowBusinessSelect) sqlScan(ctx context.Context, root *UserFollowBusinessQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ufbs.fns))
	for _, fn := range ufbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ufbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ufbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
