// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"placio-app/ent/user"
	"placio-app/ent/userfollowuser"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserFollowUser is the model entity for the UserFollowUser schema.
type UserFollowUser struct {
	config
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserFollowUserQuery when eager-loading is set.
	Edges               UserFollowUserEdges `json:"edges"`
	user_followed_users *string
	user_follower_users *string
	selectValues        sql.SelectValues
}

// UserFollowUserEdges holds the relations/edges for other nodes in the graph.
type UserFollowUserEdges struct {
	// Follower holds the value of the follower edge.
	Follower *User `json:"follower,omitempty"`
	// Followed holds the value of the followed edge.
	Followed *User `json:"followed,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// FollowerOrErr returns the Follower value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserFollowUserEdges) FollowerOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.Follower == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.Follower, nil
	}
	return nil, &NotLoadedError{edge: "follower"}
}

// FollowedOrErr returns the Followed value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserFollowUserEdges) FollowedOrErr() (*User, error) {
	if e.loadedTypes[1] {
		if e.Followed == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.Followed, nil
	}
	return nil, &NotLoadedError{edge: "followed"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserFollowUser) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userfollowuser.FieldID:
			values[i] = new(sql.NullString)
		case userfollowuser.ForeignKeys[0]: // user_followed_users
			values[i] = new(sql.NullString)
		case userfollowuser.ForeignKeys[1]: // user_follower_users
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserFollowUser fields.
func (ufu *UserFollowUser) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userfollowuser.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				ufu.ID = value.String
			}
		case userfollowuser.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_followed_users", values[i])
			} else if value.Valid {
				ufu.user_followed_users = new(string)
				*ufu.user_followed_users = value.String
			}
		case userfollowuser.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_follower_users", values[i])
			} else if value.Valid {
				ufu.user_follower_users = new(string)
				*ufu.user_follower_users = value.String
			}
		default:
			ufu.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserFollowUser.
// This includes values selected through modifiers, order, etc.
func (ufu *UserFollowUser) Value(name string) (ent.Value, error) {
	return ufu.selectValues.Get(name)
}

// QueryFollower queries the "follower" edge of the UserFollowUser entity.
func (ufu *UserFollowUser) QueryFollower() *UserQuery {
	return NewUserFollowUserClient(ufu.config).QueryFollower(ufu)
}

// QueryFollowed queries the "followed" edge of the UserFollowUser entity.
func (ufu *UserFollowUser) QueryFollowed() *UserQuery {
	return NewUserFollowUserClient(ufu.config).QueryFollowed(ufu)
}

// Update returns a builder for updating this UserFollowUser.
// Note that you need to call UserFollowUser.Unwrap() before calling this method if this UserFollowUser
// was returned from a transaction, and the transaction was committed or rolled back.
func (ufu *UserFollowUser) Update() *UserFollowUserUpdateOne {
	return NewUserFollowUserClient(ufu.config).UpdateOne(ufu)
}

// Unwrap unwraps the UserFollowUser entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ufu *UserFollowUser) Unwrap() *UserFollowUser {
	_tx, ok := ufu.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserFollowUser is not a transactional entity")
	}
	ufu.config.driver = _tx.drv
	return ufu
}

// String implements the fmt.Stringer.
func (ufu *UserFollowUser) String() string {
	var builder strings.Builder
	builder.WriteString("UserFollowUser(")
	builder.WriteString(fmt.Sprintf("id=%v", ufu.ID))
	builder.WriteByte(')')
	return builder.String()
}

// UserFollowUsers is a parsable slice of UserFollowUser.
type UserFollowUsers []*UserFollowUser
