// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"placio-app/ent/business"
	"placio-app/ent/businessaccountsettings"
	"placio-app/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// BusinessAccountSettingsQuery is the builder for querying BusinessAccountSettings entities.
type BusinessAccountSettingsQuery struct {
	config
	ctx                 *QueryContext
	order               []businessaccountsettings.OrderOption
	inters              []Interceptor
	predicates          []predicate.BusinessAccountSettings
	withBusinessAccount *BusinessQuery
	withFKs             bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BusinessAccountSettingsQuery builder.
func (basq *BusinessAccountSettingsQuery) Where(ps ...predicate.BusinessAccountSettings) *BusinessAccountSettingsQuery {
	basq.predicates = append(basq.predicates, ps...)
	return basq
}

// Limit the number of records to be returned by this query.
func (basq *BusinessAccountSettingsQuery) Limit(limit int) *BusinessAccountSettingsQuery {
	basq.ctx.Limit = &limit
	return basq
}

// Offset to start from.
func (basq *BusinessAccountSettingsQuery) Offset(offset int) *BusinessAccountSettingsQuery {
	basq.ctx.Offset = &offset
	return basq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (basq *BusinessAccountSettingsQuery) Unique(unique bool) *BusinessAccountSettingsQuery {
	basq.ctx.Unique = &unique
	return basq
}

// Order specifies how the records should be ordered.
func (basq *BusinessAccountSettingsQuery) Order(o ...businessaccountsettings.OrderOption) *BusinessAccountSettingsQuery {
	basq.order = append(basq.order, o...)
	return basq
}

// QueryBusinessAccount chains the current query on the "business_account" edge.
func (basq *BusinessAccountSettingsQuery) QueryBusinessAccount() *BusinessQuery {
	query := (&BusinessClient{config: basq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := basq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := basq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(businessaccountsettings.Table, businessaccountsettings.FieldID, selector),
			sqlgraph.To(business.Table, business.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, businessaccountsettings.BusinessAccountTable, businessaccountsettings.BusinessAccountColumn),
		)
		fromU = sqlgraph.SetNeighbors(basq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BusinessAccountSettings entity from the query.
// Returns a *NotFoundError when no BusinessAccountSettings was found.
func (basq *BusinessAccountSettingsQuery) First(ctx context.Context) (*BusinessAccountSettings, error) {
	nodes, err := basq.Limit(1).All(setContextOp(ctx, basq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{businessaccountsettings.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (basq *BusinessAccountSettingsQuery) FirstX(ctx context.Context) *BusinessAccountSettings {
	node, err := basq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BusinessAccountSettings ID from the query.
// Returns a *NotFoundError when no BusinessAccountSettings ID was found.
func (basq *BusinessAccountSettingsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = basq.Limit(1).IDs(setContextOp(ctx, basq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{businessaccountsettings.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (basq *BusinessAccountSettingsQuery) FirstIDX(ctx context.Context) int {
	id, err := basq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BusinessAccountSettings entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BusinessAccountSettings entity is found.
// Returns a *NotFoundError when no BusinessAccountSettings entities are found.
func (basq *BusinessAccountSettingsQuery) Only(ctx context.Context) (*BusinessAccountSettings, error) {
	nodes, err := basq.Limit(2).All(setContextOp(ctx, basq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{businessaccountsettings.Label}
	default:
		return nil, &NotSingularError{businessaccountsettings.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (basq *BusinessAccountSettingsQuery) OnlyX(ctx context.Context) *BusinessAccountSettings {
	node, err := basq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BusinessAccountSettings ID in the query.
// Returns a *NotSingularError when more than one BusinessAccountSettings ID is found.
// Returns a *NotFoundError when no entities are found.
func (basq *BusinessAccountSettingsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = basq.Limit(2).IDs(setContextOp(ctx, basq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{businessaccountsettings.Label}
	default:
		err = &NotSingularError{businessaccountsettings.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (basq *BusinessAccountSettingsQuery) OnlyIDX(ctx context.Context) int {
	id, err := basq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BusinessAccountSettingsSlice.
func (basq *BusinessAccountSettingsQuery) All(ctx context.Context) ([]*BusinessAccountSettings, error) {
	ctx = setContextOp(ctx, basq.ctx, "All")
	if err := basq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BusinessAccountSettings, *BusinessAccountSettingsQuery]()
	return withInterceptors[[]*BusinessAccountSettings](ctx, basq, qr, basq.inters)
}

// AllX is like All, but panics if an error occurs.
func (basq *BusinessAccountSettingsQuery) AllX(ctx context.Context) []*BusinessAccountSettings {
	nodes, err := basq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BusinessAccountSettings IDs.
func (basq *BusinessAccountSettingsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if basq.ctx.Unique == nil && basq.path != nil {
		basq.Unique(true)
	}
	ctx = setContextOp(ctx, basq.ctx, "IDs")
	if err = basq.Select(businessaccountsettings.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (basq *BusinessAccountSettingsQuery) IDsX(ctx context.Context) []int {
	ids, err := basq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (basq *BusinessAccountSettingsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, basq.ctx, "Count")
	if err := basq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, basq, querierCount[*BusinessAccountSettingsQuery](), basq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (basq *BusinessAccountSettingsQuery) CountX(ctx context.Context) int {
	count, err := basq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (basq *BusinessAccountSettingsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, basq.ctx, "Exist")
	switch _, err := basq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (basq *BusinessAccountSettingsQuery) ExistX(ctx context.Context) bool {
	exist, err := basq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BusinessAccountSettingsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (basq *BusinessAccountSettingsQuery) Clone() *BusinessAccountSettingsQuery {
	if basq == nil {
		return nil
	}
	return &BusinessAccountSettingsQuery{
		config:              basq.config,
		ctx:                 basq.ctx.Clone(),
		order:               append([]businessaccountsettings.OrderOption{}, basq.order...),
		inters:              append([]Interceptor{}, basq.inters...),
		predicates:          append([]predicate.BusinessAccountSettings{}, basq.predicates...),
		withBusinessAccount: basq.withBusinessAccount.Clone(),
		// clone intermediate query.
		sql:  basq.sql.Clone(),
		path: basq.path,
	}
}

// WithBusinessAccount tells the query-builder to eager-load the nodes that are connected to
// the "business_account" edge. The optional arguments are used to configure the query builder of the edge.
func (basq *BusinessAccountSettingsQuery) WithBusinessAccount(opts ...func(*BusinessQuery)) *BusinessAccountSettingsQuery {
	query := (&BusinessClient{config: basq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	basq.withBusinessAccount = query
	return basq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BusinessAccountSettingsID string `json:"BusinessAccountSettingsID,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BusinessAccountSettings.Query().
//		GroupBy(businessaccountsettings.FieldBusinessAccountSettingsID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (basq *BusinessAccountSettingsQuery) GroupBy(field string, fields ...string) *BusinessAccountSettingsGroupBy {
	basq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BusinessAccountSettingsGroupBy{build: basq}
	grbuild.flds = &basq.ctx.Fields
	grbuild.label = businessaccountsettings.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BusinessAccountSettingsID string `json:"BusinessAccountSettingsID,omitempty"`
//	}
//
//	client.BusinessAccountSettings.Query().
//		Select(businessaccountsettings.FieldBusinessAccountSettingsID).
//		Scan(ctx, &v)
func (basq *BusinessAccountSettingsQuery) Select(fields ...string) *BusinessAccountSettingsSelect {
	basq.ctx.Fields = append(basq.ctx.Fields, fields...)
	sbuild := &BusinessAccountSettingsSelect{BusinessAccountSettingsQuery: basq}
	sbuild.label = businessaccountsettings.Label
	sbuild.flds, sbuild.scan = &basq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BusinessAccountSettingsSelect configured with the given aggregations.
func (basq *BusinessAccountSettingsQuery) Aggregate(fns ...AggregateFunc) *BusinessAccountSettingsSelect {
	return basq.Select().Aggregate(fns...)
}

func (basq *BusinessAccountSettingsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range basq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, basq); err != nil {
				return err
			}
		}
	}
	for _, f := range basq.ctx.Fields {
		if !businessaccountsettings.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if basq.path != nil {
		prev, err := basq.path(ctx)
		if err != nil {
			return err
		}
		basq.sql = prev
	}
	return nil
}

func (basq *BusinessAccountSettingsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BusinessAccountSettings, error) {
	var (
		nodes       = []*BusinessAccountSettings{}
		withFKs     = basq.withFKs
		_spec       = basq.querySpec()
		loadedTypes = [1]bool{
			basq.withBusinessAccount != nil,
		}
	)
	if basq.withBusinessAccount != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, businessaccountsettings.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BusinessAccountSettings).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BusinessAccountSettings{config: basq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, basq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := basq.withBusinessAccount; query != nil {
		if err := basq.loadBusinessAccount(ctx, query, nodes, nil,
			func(n *BusinessAccountSettings, e *Business) { n.Edges.BusinessAccount = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (basq *BusinessAccountSettingsQuery) loadBusinessAccount(ctx context.Context, query *BusinessQuery, nodes []*BusinessAccountSettings, init func(*BusinessAccountSettings), assign func(*BusinessAccountSettings, *Business)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*BusinessAccountSettings)
	for i := range nodes {
		if nodes[i].business_business_account_settings == nil {
			continue
		}
		fk := *nodes[i].business_business_account_settings
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(business.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "business_business_account_settings" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (basq *BusinessAccountSettingsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := basq.querySpec()
	_spec.Node.Columns = basq.ctx.Fields
	if len(basq.ctx.Fields) > 0 {
		_spec.Unique = basq.ctx.Unique != nil && *basq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, basq.driver, _spec)
}

func (basq *BusinessAccountSettingsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(businessaccountsettings.Table, businessaccountsettings.Columns, sqlgraph.NewFieldSpec(businessaccountsettings.FieldID, field.TypeInt))
	_spec.From = basq.sql
	if unique := basq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if basq.path != nil {
		_spec.Unique = true
	}
	if fields := basq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, businessaccountsettings.FieldID)
		for i := range fields {
			if fields[i] != businessaccountsettings.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := basq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := basq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := basq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := basq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (basq *BusinessAccountSettingsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(basq.driver.Dialect())
	t1 := builder.Table(businessaccountsettings.Table)
	columns := basq.ctx.Fields
	if len(columns) == 0 {
		columns = businessaccountsettings.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if basq.sql != nil {
		selector = basq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if basq.ctx.Unique != nil && *basq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range basq.predicates {
		p(selector)
	}
	for _, p := range basq.order {
		p(selector)
	}
	if offset := basq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := basq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BusinessAccountSettingsGroupBy is the group-by builder for BusinessAccountSettings entities.
type BusinessAccountSettingsGroupBy struct {
	selector
	build *BusinessAccountSettingsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (basgb *BusinessAccountSettingsGroupBy) Aggregate(fns ...AggregateFunc) *BusinessAccountSettingsGroupBy {
	basgb.fns = append(basgb.fns, fns...)
	return basgb
}

// Scan applies the selector query and scans the result into the given value.
func (basgb *BusinessAccountSettingsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, basgb.build.ctx, "GroupBy")
	if err := basgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BusinessAccountSettingsQuery, *BusinessAccountSettingsGroupBy](ctx, basgb.build, basgb, basgb.build.inters, v)
}

func (basgb *BusinessAccountSettingsGroupBy) sqlScan(ctx context.Context, root *BusinessAccountSettingsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(basgb.fns))
	for _, fn := range basgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*basgb.flds)+len(basgb.fns))
		for _, f := range *basgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*basgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := basgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BusinessAccountSettingsSelect is the builder for selecting fields of BusinessAccountSettings entities.
type BusinessAccountSettingsSelect struct {
	*BusinessAccountSettingsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bass *BusinessAccountSettingsSelect) Aggregate(fns ...AggregateFunc) *BusinessAccountSettingsSelect {
	bass.fns = append(bass.fns, fns...)
	return bass
}

// Scan applies the selector query and scans the result into the given value.
func (bass *BusinessAccountSettingsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bass.ctx, "Select")
	if err := bass.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BusinessAccountSettingsQuery, *BusinessAccountSettingsSelect](ctx, bass.BusinessAccountSettingsQuery, bass, bass.inters, v)
}

func (bass *BusinessAccountSettingsSelect) sqlScan(ctx context.Context, root *BusinessAccountSettingsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bass.fns))
	for _, fn := range bass.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bass.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bass.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
