name: Run Tests

on:
  push:
    branches:
      - master

  pull_request:
    branches:
      - master

  pull_request_target:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Wait for server
        run: sleep 30

      - name: Run tests
        run: go test -v ./...
